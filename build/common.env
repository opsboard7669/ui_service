export ENV=${ENV}
export SERVICE_NAME=${SERVICE_NAME}
export VERSION=${VERSION}
export SUBNAMESPACE=${SUBNAMESPACE}
export FORCE_BUILD=${FORCE_BUILD}
export SCAN=${SCAN}
export DEPLOY=${DEPLOY}
export IMAGE_NAME="${DOCKERHUB_USER}/${SERVICE_NAME}"